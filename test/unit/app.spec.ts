import {App} from '../../src/app';
/* TODO Can you write unit tests for the app? If not, remove this file. The code below was not removed from the original boilerplate so you know how to write unit tests.
describe('the app', () => {
  it('says hello', () => {
    expect(new App().message).toBe('Hello World!');
  });
});
*/
